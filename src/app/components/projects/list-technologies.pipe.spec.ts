import { ListTechnologiesPipe } from './list-technologies.pipe';

describe('ListTechnologiesPipe', () => {
  it('create an instance', () => {
    const pipe = new ListTechnologiesPipe();
    expect(pipe).toBeTruthy();
  });
});
