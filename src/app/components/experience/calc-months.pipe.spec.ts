import { CalcMonthsPipe } from './calc-months.pipe';

describe('CalcMonthsPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcMonthsPipe();
    expect(pipe).toBeTruthy();
  });
});
